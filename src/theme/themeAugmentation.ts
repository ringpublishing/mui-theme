// Backward-compatible augmentation barrel.
// Imports core and per-version augmentation modules.

import './config/augmentation.core';
import './config/reference/augmentation';
import './config/next/augmentation';

export {};
